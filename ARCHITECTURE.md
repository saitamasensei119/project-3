# 🏗️ Frontend Architecture

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      Browser (Client)                        │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              React App (http://localhost:5173)         │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │                                                         │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │  App.jsx (Root + Router Setup)                  │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  │              ↓                                          │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │  Pages Layer                                     │ │ │
│  │  ├──────────────────────────────────────────────────┤ │ │
│  │  │  ├─ Login.jsx                                   │ │ │
│  │  │  ├─ Register.jsx                                │ │ │
│  │  │  └─ Dashboard.jsx                               │ │ │
│  │  │     ├─ Overview Tab                             │ │ │
│  │  │     ├─ Transactions Tab                         │ │ │
│  │  │     ├─ Budgets Tab                              │ │ │
│  │  │     └─ Goals Tab                                │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  │              ↓                                          │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │  Components Layer                                │ │ │
│  │  ├──────────────────────────────────────────────────┤ │ │
│  │  │  ├─ TransactionForm.jsx                         │ │ │
│  │  │  ├─ TransactionList.jsx                         │ │ │
│  │  │  ├─ BudgetList.jsx                              │ │ │
│  │  │  ├─ GoalList.jsx                                │ │ │
│  │  │  ├─ CategoryManager.jsx                         │ │ │
│  │  │  └─ Statistics.jsx                              │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  │              ↓                                          │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │  Services Layer (API Integration)                │ │ │
│  │  ├──────────────────────────────────────────────────┤ │ │
│  │  │  src/services/api.js                            │ │ │
│  │  │  ├─ authService                                 │ │ │
│  │  │  ├─ transactionService                          │ │ │
│  │  │  ├─ categoryService                             │ │ │
│  │  │  ├─ budgetService                               │ │ │
│  │  │  └─ goalService                                 │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  │              ↓                                          │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │  Utils & Helpers                                │ │ │
│  │  ├──────────────────────────────────────────────────┤ │ │
│  │  │  ├─ helpers.js (utility functions)              │ │ │
│  │  │  └─ constants.js (configs)                      │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  │              ↓                                          │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │  Styles Layer                                    │ │ │
│  │  ├──────────────────────────────────────────────────┤ │ │
│  │  │  ├─ Auth.css                                    │ │ │
│  │  │  ├─ Dashboard.css                               │ │ │
│  │  │  ├─ Form.css                                    │ │ │
│  │  │  ├─ List.css                                    │ │ │
│  │  │  └─ Statistics.css                              │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  │                                                         │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                               │
└─────────────────────────────────────────────────────────────┘
                        ↓ HTTP Requests
                   (Fetch API)
                        ↓
┌─────────────────────────────────────────────────────────────┐
│                    Backend Server                            │
│              (http://localhost:5000)                        │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Auth Routes  │  │ Transaction  │  │ Category     │      │
│  │ - register   │  │ Routes       │  │ Routes       │      │
│  │ - login      │  │ - CRUD ops   │  │ - CRUD ops   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                               │
│  ┌──────────────┐  ┌──────────────┐                        │
│  │ Budget       │  │ Goal Routes  │                        │
│  │ Routes       │  │ - CRUD ops   │                        │
│  │ - CRUD ops   │  │ - progress   │                        │
│  └──────────────┘  └──────────────┘                        │
│                                                               │
│               ↓ Database Operations ↓                       │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │           PostgreSQL Database                       │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │  ├─ users          ├─ budgets                       │   │
│  │  ├─ transactions   ├─ goals                         │   │
│  │  └─ categories                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## Data Flow

```
User Action (Click, Input)
    ↓
Component Event Handler
    ↓
Service Layer (api.js)
    ↓
Fetch Request to Backend
    ↓
Backend Processing
    ↓
Database Operation
    ↓
Backend Response (JSON)
    ↓
Component State Update
    ↓
React Re-render
    ↓
Updated UI Displayed
    ↓
User Sees Result
```

---

## Component Hierarchy

```
App
├── BrowserRouter
│   └── Routes
│       ├── /login
│       │   └── Login
│       ├── /register
│       │   └── Register
│       └── /dashboard (Protected)
│           └── Dashboard
│               ├── Header
│               ├── Navigation Tabs
│               └── Content
│                   ├── Overview
│                   │   ├── StatisticsComponent
│                   │   ├── OverviewCards
│                   │   └── RecentTransactions
│                   ├── Transactions
│                   │   ├── TransactionForm
│                   │   └── TransactionList
│                   ├── Budgets
│                   │   └── BudgetList
│                   └── Goals
│                       └── GoalList
```

---

## State Management Flow

```
localStorage
├── token (JWT)
└── user (User Info)
    ↓ Read on App Load
    ↓ Used for Authorization
    ↓
Component State
├── Dashboard
│   ├── activeTab
│   ├── transactions
│   ├── budgets
│   └── goals
└── Forms
    ├── formData
    ├── loading
    └── error
```

---

## API Call Flow

```
1. User Submits Form
   ↓
2. handleSubmit() Called
   ↓
3. Validation Check
   ↓
4. Service Method Called
   fetch(url, {
     method: 'POST',
     headers: {
       'Authorization': 'Bearer ' + token
     },
     body: JSON.stringify(data)
   })
   ↓
5. Backend Processes Request
   ↓
6. Response Received
   ↓
7. Response Check
   if (response.ok) {
     Update State
   } else {
     Show Error
   }
   ↓
8. Component Re-renders
   ↓
9. UI Updated with Results
```

---

## Authentication Flow

```
┌─────────────┐
│   Login     │
│  Component  │
└──────┬──────┘
       │ Email + Password
       ↓
┌──────────────────┐
│ Login Form       │
│ Submission       │
└──────┬───────────┘
       │
       ↓
┌──────────────────────────┐
│ authService.login()      │
│ POST /api/auth/login     │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────┐
│ Backend Login    │
│ Processing       │
└──────┬───────────┘
       │
       ↓ (Success)
┌──────────────────────────┐
│ Return JWT Token + User  │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│ Save to localStorage:    │
│ - token                  │
│ - user                   │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│ Redirect to Dashboard    │
└──────────────────────────┘
```

---

## File Organization

```
frontend/
│
├── src/
│   ├── components/           (Reusable UI components)
│   │   ├── TransactionForm.jsx
│   │   ├── TransactionList.jsx
│   │   ├── BudgetList.jsx
│   │   ├── GoalList.jsx
│   │   ├── CategoryManager.jsx
│   │   └── Statistics.jsx
│   │
│   ├── pages/               (Page-level components)
│   │   ├── Login.jsx
│   │   ├── Register.jsx
│   │   └── Dashboard.jsx
│   │
│   ├── services/            (API integration)
│   │   └── api.js
│   │
│   ├── utils/               (Helper functions)
│   │   ├── helpers.js
│   │   └── constants.js
│   │
│   ├── styles/              (CSS files)
│   │   ├── Auth.css
│   │   ├── Dashboard.css
│   │   ├── Form.css
│   │   ├── List.css
│   │   └── Statistics.css
│   │
│   ├── App.jsx              (Root component)
│   ├── App.css
│   ├── index.css
│   └── main.jsx             (Entry point)
│
├── public/                  (Static assets)
│
├── package.json
├── vite.config.js
└── index.html
```

---

## Key Features by Component

| Component           | Features                                                    |
| ------------------- | ----------------------------------------------------------- |
| **Login**           | Email/password validation, error handling, remember session |
| **Register**        | Form validation, password confirmation, user creation       |
| **Dashboard**       | Tab navigation, 4 main sections, user greeting, logout      |
| **TransactionForm** | Category filter, date picker, amount input, description     |
| **TransactionList** | Filter by type, display categories, delete action           |
| **BudgetList**      | Month selection, progress tracking, CRUD operations         |
| **GoalList**        | Target date selection, progress update, CRUD                |
| **CategoryManager** | Type-based organization, CRUD operations                    |
| **Statistics**      | Summary cards, category breakdown, percentage               |

---

**Frontend Architecture - Complete & Production Ready!** ✅
